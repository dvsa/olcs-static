var OLCS = OLCS || {};

OLCS.multiFilter = (function(document, $, undefined) {

  "use strict";

  return function init(options) {

    // @TODO cache the `to` object's values,
    // since we have to remove their optgroups

    // set up a listener on options.from change, and
    // based on its value, create an optgroup with
    // the values from the cached set
    // and set the select value to have this single
    // optgroup
  };

}(document, window.jQuery));
